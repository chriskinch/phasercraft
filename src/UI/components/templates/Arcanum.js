import React from "react";
import 'styled-components/macro';

const Arcanum = () => {
    return (
        <div css={`
            display: flex;
        `}>
            <section>
                <h2>Arcanum</h2>
            </section>
        </div>
    );
}

export default Arcanum;